class Homework {
  String name;
  DateTime dueDate;
  String subject;
  String note;
  String where;
  String studentId;
  bool isWaiting;

  Homework({this.name, this.dueDate, this.subject, this.note, this.where, this.studentId, this.isWaiting});
}
